apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: add-response-header
config:
  add:
    headers:
      - "add-response-header: injected-by-kong"
plugin: response-transformer
